services:
  - type: web
    name: ai-email-api

    runtime: python

    plan: free

    buildCommand: pip install -r requirements.txt

    startCommand: uvicorn api.app:app --host 0.0.0.0 --port $PORT

    envVars:
      - key: GEMINI_API_KEY1_reply
        sync: false